[project]
name = "mcpdemo-filesystem"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain-mcp-adapters>=0.2.1",
    "langchain-openai>=1.1.6",
]
